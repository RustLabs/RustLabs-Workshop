I"P<h1 id="type-casting-operator">Type Casting Operator</h1>

<p>What Is Type Casting?
Type casting is when you convert the data type of the variable to some other data type.</p>

<ul>
  <li>Type Casting in Rust
In Rust, typecasting is done using the as keyword followed by the desired data type of the variable or value.</li>
</ul>

<p><img src="https://raw.githubusercontent.com/sangam14/RustLabs/master/img/type-casting.png" alt="" /></p>

<p>The following example demonstrates the use of type casting operator in a program:</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
fn main() {
    let a = 15;
    let b = (a as f64) / 2.0; 
    println!("a: {}", a);
    println!("b: {}", b);
}

</code></pre></div></div>
<p>output</p>
<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>a: 15
b: 7.5

</code></pre></div></div>
<p>📝 What data types can be type casted?</p>
<ul>
  <li>Integer can be type casted to floating-point and vice versa.</li>
  <li>Integer can be typecasted to String</li>
</ul>

<p>📝What data types cannot be type casted?</p>
<ul>
  <li>String (&amp;str) or character cannot be type casted to the data type of type integer or float.</li>
  <li>Character cannot be type casted to String type and vice versa</li>
</ul>

<p>The following code gives an error, ❌, because of the invalid type casting operation:</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fn main() {
    let a: char = 'r' ; // cannot be type casted
    let b = a as &amp;str ; 
    println!("a: {}", a);
    println!("b: {}", b);
}


</code></pre></div></div>

<h1 id="quiz">Quiz</h1>

<ol>
  <li>What is the output of the following code?</li>
</ol>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>fn main() {
    let a = 15;
    let b = (a as f32) / 3.0; // 5
    println!("a:{}",a);
    println!("b:{}",b);
}

</code></pre></div></div>

<p>A) a:15 <br />
   b:7.5 <br /></p>

<p>B) a:15 <br />
   b:5 <br /></p>

:ET